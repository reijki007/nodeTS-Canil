1. Criar um objeto em pageController.ts 
    1.1 - criar objeto menu:{}
    +   res.render('pages/page', {
                menu: {
                    all: true,
                    dog: false,
                    cat: false,
                    fishes: false
                },
                banner: {
                    title: 'Todos os animais',
                    background: 'allanimals.jpg'
                }
            });
            
    1.2 - cria uma condição no header.mustache para exibir o menu
        + usa em class=" {{#menu.all}}active{{/menu.all}} "

     <nav>
        <ul>
    +       <li class="{{#menu.all}} active {{/menu.all}}"><a href="/">Todos</a></li>
    +       <li class=""><a href="/dogs">Cachorros</a></li>
    +       <li class=""><a href="/cats">Gatos</a></li>
    +       <li class=""><a href="/fishes">Peixes</a></li>
        </ul>
    </nav>

2. cria em src uma pasta (helpers)
    1.1 - dentro de src/helpers criar
     + createMenuObject.ts

    1.2 - dentro de createMenuObject.ts 

        // cria um type para activeMenu
    +   type MenuOptions = '' | 'all' | 'dog' | 'cat' | 'fish';

        // usa MenuOtions em ( active: MenuOptions )

    +   export const createMenuObject = (activeMenu) => {
    +        // monta o return
    +   let returnObject = {
    +       all: false,
    +       dog: false,
    +       cat: false,
    +      fish: false
    +   }; 

    +   if (activeMenu != '') {
    +   // seleciona o item desejado
    +    returnObject[activeMenu] = true;
    + }   

    return returnObject;
    } 


    export deafult {
        createMenuObject
    };

    1.3 - import em pageController (createMenuObject)
    +   import {createMenuObject} from '../helpers/createMenuObject';